//IIFES!!

var colors = [];

var choice = (function() {
    var colors = ["red", "orange", "yellow","green","blue","indigo","violet"]
    var color = colors[Math.floor(Math.random()*100) % colors.length]
    console.log("The color is: ", color)
    return color
})()


console["log"]("The color really is: ", choice)

console["log"](colors)
{
    let choice = "New choice!"
}
console.log(choice)

let hello = "hello";

function hello(num) {
    console.log("hello!");
}

console.log(hello);